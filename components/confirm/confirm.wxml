<view class="container" wx:if="{{visible}}">
<view class="confirm">
<textarea placeholder='{{placeholder}}' value="{{_value}}" bindinput="onInput">
</textarea>
<view class="actions">
<view hover-class='active' class="selected" bindtap='onConfirm'>确认</view>
<view class="line"></view>
<view hover-class='active' bindtap='onCancle'>取消</view>
</view>
</view>
</view>